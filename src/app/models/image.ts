export class Image {
  constructor(url: string, text: string, description: string) {
    this.url = url;
    this.text = text;
    this.description = description;

  }

  url: string;
  text: string;
  description: string;
}
